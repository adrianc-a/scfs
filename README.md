#scfs: Sound Cloud File System
[FUSE](http://fuse.sourceforge.net) for sound cloud

##building/running
Can build with `make all` executable will be in `src`

run with `./scfs [FUSE opts] CLIENT_ID username`

